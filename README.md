# QRYO Backend

QRYO is a vendor-agnostic quantum job aggregator.
Run quantum jobs from a single API with pay-per-use model.

## Features
- Job submission
- Job status tracking
- Provider routing (simulated / quantum-ready)
- REST API (FastAPI)

## Run
This service is designed to be deployed via Docker (Render compatible).
